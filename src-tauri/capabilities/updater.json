{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Signed app updates are available only in the main window",
  "windows": ["main"],
  "permissions": ["updater:default"]
}
